<nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light" id="nav-header">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>"> 
      <%= image_tag("logo.png", width: "144px", height: "44px") %>
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="#"> Products</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">About</a>
        </li>
        <li class="nav-item">
          <% if user_signed_in? %>
            <a class="nav-link" href="<%= destroy_user_session_path %>" method="delete" data-confirm="Are you sure?">Sign out</a>
            
          <% else %>
            <a class="nav-link" href="<%= new_user_session_path %>">Sign In</a>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
<br> 